<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.wecode</groupId>
        <artifactId>we-code</artifactId>
        <version>1.0-SNAPSHOT</version>
    </parent>

    <artifactId>cli</artifactId>
    <name>we-code-cli</name>
    <description>Command-line entry point</description>

    <dependencies>
        <dependency>
            <groupId>info.picocli</groupId>
            <artifactId>picocli</artifactId>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.dataformat</groupId>
            <artifactId>jackson-dataformat-yaml</artifactId>
        </dependency>
        <dependency>
            <groupId>org.wecode</groupId>
            <artifactId>agent</artifactId>
        </dependency>
        <dependency>
            <groupId>org.wecode</groupId>
            <artifactId>session</artifactId>
        </dependency>
        <dependency>
            <groupId>org.wecode</groupId>
            <artifactId>permission</artifactId>
        </dependency>
        <dependency>
            <groupId>org.wecode</groupId>
            <artifactId>llm</artifactId>
        </dependency>
        <dependency>
            <groupId>org.wecode</groupId>
            <artifactId>tools</artifactId>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <resources>
            <resource>
                <!-- 将被 .gitignore 排除的本地运行配置作为 CLI Jar 资源打包。 -->
                <directory>${maven.multiModuleProjectDirectory}</directory>
                <includes>
                    <include>wecode.yml</include>
                </includes>
            </resource>
        </resources>
        <!-- 统一产物名，便于安装脚本将 target/wecode.jar 复制到全局命令目录。 -->
        <finalName>wecode</finalName>
        <plugins>
            <!-- 将 CLI、本项目模块及全部运行时依赖合并为可独立运行的 Jar。 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.6.0</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                        <configuration>
                            <!-- 使用合并后的 Jar 替换普通 Jar，产物路径固定为 target/wecode.jar。 -->
                            <shadedArtifactAttached>false</shadedArtifactAttached>
                            <createDependencyReducedPom>false</createDependencyReducedPom>
                            <transformers>
                                <!-- 写入 java -jar 的应用入口。 -->
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                                    <mainClass>org.wecode.cli.Main</mainClass>
                                </transformer>
                                <!-- 合并 SPI 声明，确保 Flyway 等通过 ServiceLoader 发现的实现不会在打包后丢失。 -->
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer"/>
                            </transformers>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>3.5.0</version>
                <configuration>
                    <mainClass>org.wecode.cli.Main</mainClass>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.5.2</version>
            </plugin>
        </plugins>
    </build>

</project>
