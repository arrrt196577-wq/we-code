<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.wecode.session.persistence.mapper.MessagePersistenceMapper">

    <resultMap id="messageRecordResultMap" type="org.wecode.session.persistence.entity.MessageRecord">
        <constructor>
            <arg column="session_id" javaType="java.lang.String"/>
            <arg column="seq" javaType="long"/>
            <arg column="agent_round_no" javaType="java.lang.Long"/>
            <arg column="role" javaType="org.wecode.llm.model.Role"/>
            <arg column="content" javaType="java.lang.String"/>
            <arg column="tool_call_id" javaType="java.lang.String"/>
            <arg column="created_at" javaType="long"/>
            <arg column="payload_json" javaType="java.lang.String"/>
        </constructor>
    </resultMap>

    <insert id="insert" parameterType="org.wecode.session.persistence.entity.MessageRecord">
        INSERT INTO messages (
            session_id, seq, agent_round_no, role,
            content, tool_call_id, created_at, payload_json
        ) VALUES (
            #{sessionId}, #{seq}, #{agentRoundNo}, #{role},
            #{content}, #{toolCallId}, #{createdAt}, #{payloadJson}
        )
    </insert>

    <select id="findAllBySessionId" resultMap="messageRecordResultMap">
        SELECT session_id, seq, agent_round_no, role,
               content, tool_call_id, created_at, payload_json
        FROM messages
        WHERE session_id = #{sessionId}
        ORDER BY seq ASC
    </select>

    <select id="findRecentBySessionId" resultMap="messageRecordResultMap">
        SELECT session_id, seq, agent_round_no, role,
               content, tool_call_id, created_at, payload_json
        FROM (
            SELECT session_id, seq, agent_round_no, role,
                   content, tool_call_id, created_at, payload_json
            FROM messages
            WHERE session_id = #{sessionId}
            ORDER BY seq DESC
            LIMIT #{limit}
        )
        ORDER BY seq ASC
    </select>
</mapper>
